#pragma once

#include "Point.h"

struct Point;
struct Dart;
struct Vertex;
struct Edge;
struct Face;
struct Volume;

struct Dart {
  // involutions:

  // cells:

};

struct Vertex {
  // the coordinates of this vertex:
  Point barycenter;

  // a dart incident to this Vertext:

};

struct Edge {
  // a dart incident to this Edge:

};

struct Face {
  // a dart incident to this Face:

};

struct Volume {
  // a dart incident to this Volume:

};